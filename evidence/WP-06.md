# WP-06 evidence — iMessage Mac capture connector (`brain connect imessage`)

## 2026-08-29 common-contract hardening

The real command adapter now returns submitted, accepted, and refused
conversation counts plus the shared source outcome. A Worker credential refusal
is `partial`, never completion-shaped, and only accepted conversations count as
present in `brain load`. Dry-run sessionization reports the exact would-submit
document count while sending no batch and writing no capture state. An explicit
`--limit` or credential refusal remains warning-shaped and partial, with a
redacted refusal reason on the sync receipt. This is fixture proof only; no
accepted real `chat.db` receipt exists.

Date: 2026-08-27. Branch `wave1/wp06-imessage`, built in an isolated worktree
off `wave0/connector-gaps`. Written to the tracked `evidence/` directory per
the plan's durability note (planning/ is gitignored and would not survive a
fresh clone).

## What shipped

- `connectors/imessage.mjs` — the capture core: incremental chat.db reader
  (ROWID watermark, GUID identity), the attributedBody typedstream parser
  ported to Buffer slicing (all three length-prefix branches + the
  mostly-replacement-characters bail-out), iMessage-vs-SMS classification off
  `handle.service` case-insensitively, atomic owner-only state carrying BOTH
  the watermark and the sessionizer snapshot, immediate re-poll on a full
  5000-row page, a stale-session sweep so dormant threads become searchable,
  and access failures NAMED: `full_disk_access_denied` (EPERM/EACCES) distinct
  from `chat_db_missing` (ENOENT) distinct from a locked/unreadable database.
  The snapshot-copy fallback classifies its own permission errors and copies
  into an owner-only temp dir it removes on close (the reference left a
  world-readable /tmp copy behind).
- `operations/drive-scheduler.mjs` — generalized into a parameterized
  connector scheduler (spec object). The Drive spec reproduces prior behavior
  exactly, including a byte-identical config-hash payload; every previously
  exported Drive-named function still exports; all 69 existing drive-scheduler
  tests pass WITHOUT EDITS.
- `operations/imessage-scheduler.mjs` — the every-minute
  StartCalendarInterval LaunchAgent (short ticks + lockf, per the plan's
  decision (a); NOT a KeepAlive daemon), whose tick child is
  `brain ingest <manifest> --from imessage`.
- `brain.mjs` — `brain ingest --from imessage` (also `--dry-run`, `--limit`,
  `--reset`, `--chat-db`, `--flush-sessions`), `brain connect imessage`
  (FDA verified by a REAL read before anything installs; initial history load
  in the foreground with counts; then the agent + freshness expectation), and
  `brain disconnect imessage` — the first disconnect verb, removal reachable
  even when the corpus flag is off, with a best-effort open-session flush and
  expectation clear. Envelope source_type is rewritten to the load's name so
  `brain forget --source imessage` genuinely scopes to these documents.
- Worker `SOURCE_KINDS` accepts `imessage`; support journal learns the
  `disconnect` command and `imessage` source; manifest schema + template
  document `corpora.imessage` and `operations.imessage_capture_cron`.
- Matrix row + section moved to Built with the Mac-only and scheduler-tick
  truths stated; CHANGELOG 0.1.22 in owner voice; version pinned everywhere
  `test/current-version.test.mjs` checks.

## Decisions taken (as directed by the plan/brief)

- Scheduler = generalized drive-scheduler (option a), short ticks every
  minute. No KeepAlive daemon, no ported bash locking.
- No Postgres/person-graph logic from the reference was ported; the ONLY
  output path is `MessageSessionizer` envelopes through the shared
  `/api/admin/brain/ingest/batch` push (so the worker's credential gate runs
  on every document — nothing bypasses it).
- Message GUID is the durable source identity (session documents are keyed
  by their first message's GUID).
- All fixtures are invented (Alex Rivera / Jordan Lee / +1555… personas,
  consistent with test/fixtures); the synthetic chat.db is built inside the
  tests with node:sqlite. No real chat data anywhere, including comments.

## Test evidence (pasted verbatim)

### Capture core — `node --no-warnings test/imessage-capture.test.mjs` (exit 0)
```
PASS  a short single-length-byte body decodes exactly
PASS  a long emoji body behind the 0x81 two-byte length decodes exactly
PASS  the 0x82 four-byte length branch decodes exactly
PASS  the 0x83 six-byte length branch decodes exactly
PASS  NSMutableString-marked bodies decode too
PASS  a decode that is mostly replacement characters bails out to null
PASS  a blob with no NSString marker returns null
PASS  a marker with no '+' separator returns null
PASS  empty and missing blobs return null
PASS  a truncated 0x81 length field returns null instead of throwing
PASS  nanosecond Mac absolute time converts to the exact ISO instant
PASS  second-precision Mac absolute time (older databases) converts too
PASS  zero and null timestamps are null, never the epoch
PASS  handle.service iMessage classifies as imessage case-insensitively
PASS  every other service value classifies as sms, including null
PASS  a missing chat.db is named chat_db_missing, with the path in the message
PASS  a permission-denied chat.db is named full_disk_access_denied, distinctly from missing
PASS  openChatDbReadOnly surfaces the denial as a typed ChatDbAccessError
PASS  after access is restored the same path probes ok
PASS  the FDA walkthrough names the exact binary and the version-manager fragility
PASS  the default chat.db path is the per-user Messages database
PASS  fetch returns every row after the watermark in ROWID order
PASS  an inbound row maps GUID, ISO time, platform and raw-handle speaker
PASS  an outbound row is direction out with no sender name (the owner label speaks)
PASS  an SMS-service handle maps the row to platform sms
PASS  a row with an empty text column decodes its attributedBody instead
PASS  the watermark excludes already-seen rows exactly
PASS  the initial load reports honest counts: rows seen, pushed, skipped
PASS  three conversations on the same day become three session documents
PASS  each session document is keyed by its first message's GUID (the durable id)
PASS  the iMessage DM session carries platform imessage and both speakers
PASS  the SMS thread's session is tagged platform sms end to end
PASS  the attributedBody-decoded text is inside the session document
PASS  no session remained open after the stale sweep
PASS  the state file carries the watermark and an empty snapshot, owner-only
PASS  re-listing the tapback row never happens: the watermark passed it
PASS  an incremental pass reads only rows after the watermark
PASS  a conversation still inside its gap window stays open, not sent
PASS  the open session rides in the state snapshot with the watermark
PASS  the restarted process continues the same session from its snapshot
PASS  nothing was double-read on resume
PASS  a mid-run ingest outage propagates instead of being swallowed
PASS  the watermark stopped exactly at the last durable page
PASS  the resumed run finishes the backlog with zero gaps
PASS  every message row is represented exactly once across the interrupted and resumed runs
PASS  a GUID never keys two different documents (idempotent by identity)
PASS  a full page triggers an immediate next read until a short page proves caught-up
PASS  a rebuild from zero re-derives the identical session identities (GUID idempotency)
PASS  maxRows bounds the pass and leaves the watermark resumable
PASS  a conversation that just happened stays open at first
PASS  a later tick with no new messages closes and sends the dormant session
PASS  a flush-only pass closes every open session without touching chat.db
PASS  a dry run sends nothing and leaves the state file untouched
PASS  a corrupt state file resets cleanly instead of aborting capture
PASS  a wrong-shaped state file also resets cleanly
PASS  saveCaptureState creates parent directories and round-trips
PASS  a stale sweep inside the gap window closes nothing
PASS  a stale sweep past the gap window closes every dormant session
PASS  the page-size constant matches the reference scan unit
PASS  openChatDbReadOnly reads the synthetic database directly

imessage capture: all 60 tests passed
```

### Scheduler — `node test/imessage-scheduler.test.mjs` (exit 0)
```
PASS  the label is standard, client-scoped, and distinct from Drive's
PASS  the default schedule is every minute, expressed as one empty calendar entry
PASS  an every-minute schedule derives a one-minute freshness expectation
PASS  the LaunchAgent invokes the iMessage scheduler runner with a configuration binding
PASS  logs and the single-instance lock are iMessage-named under .brain
PASS  the plist contains no credential material and no Drive arguments
PASS  the manifest cron knob overrides the built-in default
PASS  the generalization leaves Drive's identity exactly as before
PASS  the two connectors coexist with fully distinct plists, logs and locks
PASS  non-macOS scheduling fails with the honest Mac-only statement
PASS  corpora.imessage.enabled must be declared before install
PASS  token-free capture requires a manifest domain at install time
PASS  a bad capture cron is rejected naming the iMessage knob, not Drive's
PASS  install atomically writes a private LaunchAgent plist
PASS  install enables and bootstraps the user service
PASS  the generated definition passes macOS's native plist parser
PASS  status reports installed, loaded, running, and run history
PASS  status proves the loaded definition matches the current manifest
PASS  remove unloads and removes only the plist
PASS  status remains reachable after the iMessage corpus is disabled
PASS  remove remains reachable after the iMessage corpus is disabled
PASS  a tick invokes lockf around brain ingest --from imessage
PASS  the tick's child environment is scrubbed of every credential
PASS  a successful tick reports complete
PASS  lock contention skips a tick with the iMessage busy reason
PASS  a post-install manifest change stops the tick before any child runs
PASS  the spec's Mac-only truth is stated in the platform error itself

imessage scheduler: all 27 tests passed
```

### Drive scheduler unchanged — `node test/drive-scheduler.test.mjs` (exit 0)
```
PASS  remove preserves a bounded scheduler audit trail
PASS  status catches a manifest schedule edit that has not been reinstalled
PASS  status remains reachable after Drive or its cron is disabled
PASS  remove remains reachable after Drive or its cron is disabled
PASS  a failed replacement restores and reloads the previous plist
PASS  a failed rollback is surfaced alongside the replacement failure

drive scheduler: all 69 tests passed
```
(The drive-scheduler test file itself was NOT edited in this work package —
`git log --oneline -- test/drive-scheduler.test.mjs` shows no WP-06 commit.)

### CLI wiring — `node --no-warnings test/imessage-ingest.test.mjs` (exit 0)
```
PASS  the capture pass reads the synthetic history and reports counts
PASS  both conversations were sent as session documents keyed by first GUID
PASS  every document carries source_type imessage, so forget --source imessage scopes to it
PASS  the SMS thread stays tagged platform sms inside the imessage source
PASS  the owner's display name speaks for outbound messages
PASS  an indexing receipt opened and a ready receipt closed the run, kind imessage
PASS  the ready receipt carries the document counts
PASS  capture state landed beside the manifest under the source's name
PASS  a second pass is incremental: zero rows re-read, zero documents re-sent
PASS  a credential-gate refusal is counted and the run still completes
PASS  the failed run closed its receipt as an error
PASS  the next run retries exactly the unadvanced rows
PASS  a non-Mac machine gets the honest Mac-only statement, not a file error
PASS  connect refuses honestly when Full Disk Access is denied, and installs nothing
PASS  connect runs the initial load before installing the agent
PASS  connect sets the every-minute freshness expectation on the brain
PASS  connect returns the installed plan
PASS  connect requires corpora.imessage.enabled in the install record first
PASS  disconnect removes the LaunchAgent through the scheduler module
PASS  disconnect flushes the open session so the dormant thread becomes searchable
PASS  the flush is a flush, not a capture: no chat.db rows were re-read
PASS  disconnect clears the freshness expectation
PASS  disconnect still removes the agent when the brain is unreachable
```

### Full suite — `npm test`

Run with stdout+stderr redirected to a log and the exit code captured to a
file, then read back from the file (never piped through tail):

```
$ npm test > wp06-npm-test.log 2>&1; echo $? > wp06-npm-test.exit
$ cat wp06-npm-test.exit
0
$ grep -c '^PASS' wp06-npm-test.log ; grep -c '^FAIL' wp06-npm-test.log
2133
0
$ tail -3 wp06-npm-test.log
PASS  capability-link safety advances the durable gate version

secret-scan v4 (js): all tests passed
```

The `node --test` runner blocks inside the chain (eval suite, webauthn/
sessions/owner-auth, release-coverage) sum to: tests 140, pass 140, fail 0.

## What could NOT be proven in this environment, and why

1. **A genuine launchd TCC (Full Disk Access) denial.** TCC decisions are
   per-binary, made by macOS for the real launchd spawn chain; no test can
   grant or revoke them. The EPERM/EACCES class the classifier keys on is
   exercised through a directory the test user cannot traverse, which
   produces the same errno path — but the real "launchd-spawned node lacks
   the grant" event needs a real Mac session. The connect flow was built so
   this is verified live by construction: it refuses to install until a real
   read of the real chat.db succeeds.
2. **Capture against the real ~/Library/Messages/chat.db.** This session must
   not read the machine owner's actual messages, and tests never touch that
   path. Everything runs against a synthetic chat.db with the same schema
   subset and invented personas.
3. **A live worker round trip** (real credential gate, real Vectorize, real
   `brain sources` freshness rows). The batch endpoint, receipts and
   expectation calls are driven against scripted fakes returning the real
   receipt shapes — the same boundary every other connector test in this repo
   uses. The gate itself runs server-side on the shared endpoint this
   connector pushes through, so no new gate code was written or needed.
4. **The locked-database snapshot fallback against a genuinely live-locked
   db.** Its error paths (including its own EPERM naming) are tested; a real
   Messages.app holding the database open is a live-Mac-only condition.
5. **The "newly sent test message appears within one capture interval"
   acceptance bullet, end to end on a real Mac.** The pieces are proven
   separately (a tick spawns the capture child; a capture pass picks up new
   rows; the tick fires every minute), but the live composite needs a real
   Mac with FDA granted.

### The exact live verification for a real Mac (for the owner / Fable to run)

```bash
# in the client checkout, with corpora.imessage.enabled=true in the manifest
node brain.mjs connect imessage <manifest>       # walks FDA, loads history with counts, installs the agent
# send yourself a text from another device, wait ~90s
node brain.mjs sources <manifest>                # imessage row fresh, document count grown
node brain.mjs ask <manifest>                    # ask about the test text, expect a cited session
node brain.mjs disconnect imessage <manifest>    # agent removed, open sessions flushed
```

## Files touched in this package

- new: `connectors/imessage.mjs`, `operations/imessage-scheduler.mjs`,
  `test/imessage-capture.test.mjs`, `test/imessage-scheduler.test.mjs`,
  `test/imessage-ingest.test.mjs`, `evidence/WP-06.md`
- modified: `operations/drive-scheduler.mjs`, `brain.mjs`,
  `support-journal.mjs`, `worker/src/index.js`, `manifest.schema.json`,
  `templates/brain.manifest.json`, `onboarding/07-ingest-source-matrix.md`,
  `CHANGELOG.md`, `README.md`, `package.json`, `package-lock.json`,
  `test/package-privacy.test.mjs`
