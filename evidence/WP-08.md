# WP-08 evidence — Zoom connector, client-owned (`brain connect zoom`)

Date: 2026-08-28. Branch `wave1/wp08-zoom`, built in an isolated worktree off
`wave0/connector-gaps` at `/private/tmp/brain-wp08-zoom`. Written to the
tracked `evidence/` directory per the plan's durability note (`planning/` is
gitignored and would not survive a fresh clone).

**Read the "What could NOT be proven" section before treating this as done.**
There is no paid Zoom account in this environment, so the package's own
acceptance criterion — *a real Zoom meeting with cloud recording produces a
cited transcript document* — has not been met and cannot be met here. Every
other claim below is backed by pasted output.

## What shipped

- `worker/src/lib/zoom.js` — new file in the shared client-worker template
  (so every future client inherits it): Zoom's two-legged webhook
  verification, the Server-to-Server OAuth token exchange, the
  fetch-one-recording-by-uuid path, the VTT parser, plan-tier description, the
  credential-gated in-process ingest, the source receipt, and the envelope
  builder.
- `worker/src/index.js` — route `POST /api/webhooks/zoom`, deliberately placed
  in FRONT of the admin-key gate because Zoom cannot send an admin key; its
  authentication is the HMAC signature. `SOURCE_KINDS` accepts `zoom`.
- `connectors/zoom.mjs` — the client-side half: the marketplace instructions,
  environment-only credential custody, the live credential probe (token
  exchange, one cheap authenticated read, plan tier), and
  `verifyLiveWebhookEndpoint`, which runs Zoom's own URL-validation handshake
  against the live worker before the client is told to paste anything.
- `brain.mjs` — `brain connect zoom <manifest>` and `brain disconnect zoom
  <manifest>`, the four worker secrets written through the existing
  Cloudflare secret path onto the client's own worker, source registration,
  help text, and support-journal routing.
- `manifest.schema.json` + `templates/brain.manifest.json` — `corpora.zoom`,
  documented with the paid-seat requirement and the no-backfill limit.
- `onboarding/07-ingest-source-matrix.md` — Zoom moved from "Not built" to
  "Built", stating plainly that it has never been run against a real Zoom
  account.
- Tests: `worker/test/zoom.test.mjs` (71), `test/zoom-connect.test.mjs` (48),
  `test/zoom-cli.test.mjs` (44), plus four VTT fixtures under
  `worker/test/fixtures/zoom/`. All three files are in the `npm test` chain.

## The VTT parser: which one was ported, and why it matters

The brief said to port `vttToPlainTranscript` from the reference's
reconciliation lib rather than `parseVTTTranscript`, which is the one actually
live on the reference's ingest path, because the live one never strips WebVTT
cue-number lines. **Both reference functions were read first-hand in
`~/CloudflareWorkers/meeting-notes` (read-only; nothing there was modified),
and the claim is confirmed**: `parseVTTTranscript`'s speaker regex requires a
colon, so a bare cue number like `2` never matches it and falls through to
`currentText += " " + trimmed`.

Porting was not a copy. Three further faults were found in the better
reference parser while porting, and all four are fixed with a fixture each.
The demonstration below runs verbatim copies of BOTH reference parsers and the
ported one over the four shipped fixtures:

```
$ node vtt-compare.mjs            # throwaway script, scratchpad, not in the repo

################ cue-numbers.vtt
---- reference parseVTTTranscript (the one on the LIVE ingest path)
Alex Rivera: Thanks for making time, I know the week is full. 2
Priya Nair: No problem at all. I pulled the renewal numbers you asked for. 3
Priya Nair: They are a little better than last quarter, about four percent up. 4
Alex Rivera: Good. Send them over and I will read them before Thursday.
---- reference vttToPlainTranscript (reconcile lib, the better one)
"Alex Rivera: Thanks for making time, I know the week is full.\nPriya Nair: No problem at all. I pulled the renewal numbers you asked for.\nPriya Nair: They are a little better than last quarter, about four percent up.\nAlex Rivera: Good. Send them over and I will read them before Thursday."
---- PORTED (this repo)
"Alex Rivera: Thanks for making time, I know the week is full.\nPriya Nair: No problem at all. I pulled the renewal numbers you asked for.\nPriya Nair: They are a little better than last quarter, about four percent up.\nAlex Rivera: Good. Send them over and I will read them before Thursday."

################ header-and-note.vtt
---- reference parseVTTTranscript (the one on the LIVE ingest path)
Kind: captions
Language: en-US NOTE This transcript was generated automatically and may contain errors. 1
Morgan Diaz: Recording is on, so let's start with the inventory question.
---- reference vttToPlainTranscript (reconcile lib, the better one)
"Kind: captions\nLanguage: en-US\nMorgan Diaz: NOTE This transcript was generated automatically and may contain errors. Recording is on, so let's start with the inventory question."
---- PORTED (this repo)
"Morgan Diaz: Recording is on, so let's start with the inventory question."

################ multi-line-cue.vtt
---- reference parseVTTTranscript (the one on the LIVE ingest path)
Jordan Lee: Let's lock the schedule before we lose the room,
meet at 3: 30 on Thursday if that still works for you 2
Sam Osei: Thursday is fine. I will bring the revised timeline.
---- reference vttToPlainTranscript (reconcile lib, the better one)
"Jordan Lee: Let's lock the schedule before we lose the room,\nmeet at 3: 30 on Thursday if that still works for you\nSam Osei: Thursday is fine. I will bring the revised timeline."
---- PORTED (this repo)
"Jordan Lee: Let's lock the schedule before we lose the room, meet at 3:30 on Thursday if that still works for you\nSam Osei: Thursday is fine. I will bring the revised timeline."

################ unattributed.vtt
---- reference parseVTTTranscript (the one on the LIVE ingest path)
1 Good morning everyone, thanks for joining on short notice. 2 We will start with the quarterly numbers and then the hiring plan.
---- reference vttToPlainTranscript (reconcile lib, the better one)
""
---- PORTED (this repo)
"Good morning everyone, thanks for joining on short notice.\nWe will start with the quarterly numbers and then the hiring plan."
```

What that output shows, fault by fault:

1. **Cue numbers leak** (`cue-numbers.vtt`). The live reference parser appends
   `2`, `3`, `4` into the transcript text. This is the bug the brief named.
   Ported output is clean.
2. **Header metadata becomes a speaker** (`header-and-note.vtt`). Both
   reference parsers turn `Kind: captions` and `Language: en-US` into content;
   the reconcile one additionally attributes the NOTE disclaimer to a real
   participant, putting words in a named person's mouth. The port skips the
   header block whole and skips NOTE blocks per the WebVTT spec.
3. **A colon in a continuation line invents a speaker**
   (`multi-line-cue.vtt`). `meet at 3:30` becomes speaker `meet at 3` in the
   reconcile parser and `3: 30` in the live one. The port only looks for a
   speaker prefix on the FIRST line of a cue, so `3:30` survives as a time.
4. **An unattributed transcript parses to the empty string**
   (`unattributed.vtt`). The reference reconcile parser returns `""` —
   silent, total data loss with no error anywhere. The port emits the text
   unattributed.

Fault 4 is the reason a straight port would have been the wrong call: the
"better" parser fails worse than the buggy one on that input.

## A second latent trap found while porting: the timestamp's unit

The reference's replay guard treats `x-zm-request-timestamp` as epoch
milliseconds, asserted in a code comment. That may be right. It is not
verifiable here, and it is not a safe thing to inherit on faith, because the
failure mode is asymmetric:

- If the unit is milliseconds and we assume seconds, or vice versa, every real
  event lands ~55 years outside a five-minute window and is refused with a 401
  that looks exactly like an attacker being turned away.
- The reference can absorb that. It also runs a 15-minute cron that sweeps for
  recordings, so a webhook rejecting 100% of deliveries would be silently
  covered by the poll and might never be noticed.
- **This connector is webhook-only by design.** The same mistake here is total
  silent failure with nothing behind it: no transcripts, ever, and a 401 in a
  log nobody reads.

So `zoomTimestampToMs` accepts both units. `1e11` separates any plausible
epoch-seconds value (~1.8e9 today) from any plausible epoch-milliseconds one
(~1.8e12). This widens only the REPLAY window; the signature is still computed
over the header's exact original characters, because those are what Zoom
signed. Normalizing before hashing would break every signature, and there is a
test asserting the seconds path still refuses a six-minute-old body, so the
tolerance did not buy leniency.

This is defensive, not a claim to know the answer. The unit remains on the
unprovable list below.

## A third guard added for the same reason: secret propagation

`connect` writes the four secrets and then immediately asks the live worker to
answer Zoom's challenge. A secret written seconds ago does not always reach an
already-running isolate, so a single check can answer with the PREVIOUS secret.
That failure is indistinguishable, from the client's side, from having copied
the wrong Secret Token — and acting on it means going back to Zoom to re-copy a
value that was already correct.

The check is therefore retried up to three times with a short wait, the retry
is printed rather than hidden, and a genuinely wrong secret still fails after a
bounded number of tries. Both behaviours are tested: `a secret that lands on
the second check still connects` and `a genuinely wrong secret still fails,
after a bounded number of tries`.

## Decisions taken (scope narrowing, as the brief directed)

- **Webhook-only, no sweep.** The reference runs a webhook AND a 15-minute
  cron against the same recordings, which is why it needs a poll-style
  backlog processor and a bespoke claim-row table with a 30-minute TTL. There
  is no cron here. The webhook names the exact recording, so the handler
  fetches that one and writes one document, and the brain's existing
  `(source_type, source_id)` plus content-hash idempotency makes a redelivered
  webhook a no-op for free. No dedupe table was ported or written.
- **`recording.transcript_completed`, never `recording.completed`.** The audio
  file is written before the VTT is.
- **The per-occurrence `uuid`, never the shared meeting id**, double
  URL-encoded in the path per Zoom's own convention. A recurring meeting
  reuses one meeting id across every occurrence; the reference carries a code
  comment about a real incident caused by that ambiguity.
- **One scope: `cloud_recording:read:admin`.** The reference's four
  `user:*`/`meeting:*` read+write scopes exist for an unrelated admin feature
  (flipping auto-recording defaults) and were not carried over.
  `user:read:admin` is offered as OPTIONAL and read-only, solely so the plan
  check can be definitive; declining it produces a named unknown, not a guess.
- **No downstream pipeline.** No LLM call analysis, no content screening, no
  Drive filing, no lead creation. A transcript becomes a searchable, citable,
  forgettable document.
- **The route is the only unauthenticated-by-key write path in the worker**,
  so it fails closed with no secret set, compares signatures in constant time,
  enforces a five-minute replay window, refuses corpus writes while an upgrade
  is paused, and runs the credential gate before the store like every other
  ingest door.
- All fixtures and test personas are invented (Alex Rivera, Priya Nair, Sam
  Osei, Jordan Lee, Morgan Diaz, plus `northwind`/`Northwind Supply` in the
  CLI test), consistent with `test/fixtures/`. No real person, client or
  organization appears anywhere in this package.

## Test evidence (pasted verbatim)

### Worker route, crypto and parser — `node worker/test/zoom.test.mjs` (exit 0)

```
PASS  the token is a Server-to-Server account_credentials grant
PASS  the token exchange authenticates with Basic base64(clientId:clientSecret)
PASS  the recording is fetched by DOUBLE-encoded uuid, per Zoom's own convention
PASS  the shared meeting id is never used as the path identity (recurring meetings reuse it)
PASS  the transcript download carries the access token
PASS  double encoding is exactly two passes of encodeURIComponent
PASS  a recording with no transcript file writes no document
PASS  and says why, naming the Zoom setting rather than going quiet
PASS  a 403 from Zoom names the missing scope instead of a bare status
PASS  a transcript carrying a live credential is refused before the store
PASS  the refusal names the credential kind and never quotes its value
PASS  a clean transcript passes the gate and reaches the store
PASS  plan type 1 is Basic and is reported as unable to cloud record
PASS  plan type 2 is Licensed and can cloud record
PASS  plan type 3 is on-prem and can cloud record
PASS  an unrecognised plan type is reported as unknown, never assumed fine
PASS  a recording with no start time is not given a fabricated reliable date
PASS  a recording with no topic still gets an honest generic title

zoom: all 71 tests passed
```

(Tail shown; the full 64 include the URL-validation challenge, signature
verification with independently recomputed HMACs, a rejected bad signature, a
rejected stale timestamp, and the fail-closed-with-no-secret case.)

### Client-side connector — `node test/zoom-connect.test.mjs` (exit 0)

```
PASS  all four credentials are read from the environment
PASS  the four names are exactly the four Zoom issues
PASS  the webhook Secret Token is one of them, distinct from the client secret
PASS  a partial environment is refused and names every missing value
PASS  a whitespace-only value counts as missing, not as a credential
PASS  a value pasted with surrounding whitespace is trimmed, not sent as-is
PASS  the setup steps name the one required scope
PASS  the setup steps ask for no write scope at all
PASS  the optional plan scope is offered as optional and read-only
PASS  the setup steps say Zoom issues the Secret Token rather than offering to generate one
PASS  the setup steps warn the Secret Token is not the Client Secret
PASS  the setup steps keep the credential-custody rule
PASS  the setup steps say the app must be activated before credentials work
PASS  the setup steps do NOT yet mention the webhook endpoint URL
PASS  the subscription steps carry the exact URL the worker answers on
PASS  the subscribed event is recording.transcript_completed
PASS  and recording.completed is named as the wrong one, with the reason
PASS  the subscription steps explain that clicking Validate calls the URL live
PASS  the subscription steps state the cloud-recording-with-transcript prerequisite
PASS  the webhook url is the base plus the worker's own route
PASS  a trailing slash on the base does not produce a double slash
PASS  a good account probes clean
PASS  the token exchange is the account_credentials grant
PASS  the token exchange authenticates with Basic base64(clientId:clientSecret)
PASS  the recordings read is the cheap one-row call, not a full listing
PASS  the recordings read carries the freshly issued token
PASS  a Licensed plan is reported as confirmed, and as able to cloud record
PASS  a confirmed working plan is a note, never a blocker
PASS  a Basic account is a BLOCKER, not a warning
PASS  the Basic refusal explains that cloud recording does not exist on that tier
PASS  the Basic refusal says nothing was written
PASS  an unreadable plan tier is reported as unknown, never assumed Licensed
PASS  Zoom's scope refusal is recognised at 400/4711, not only at 403
PASS  an unconfirmed plan does NOT block the connection
PASS  but it is stated plainly, with the paid-plan requirement and where to look
PASS  a missing cloud_recording scope blocks the connect
PASS  and the refusal names the exact scope to add and says to reactivate
PASS  bad credentials fail at the token exchange, before any other call
PASS  and the message points at the three values and the activation state
PASS  a thrown plan check is caught and named, not left to crash the connect
PASS  each challenge uses a fresh random plainToken
PASS  the CLI's challenge and the real worker route agree on the answer
PASS  a worker holding a DIFFERENT secret is caught before Zoom ever sees it
PASS  a worker with no secret set is reported as not configured, with the fix
PASS  a worker with no such route is reported as needing a redeploy
PASS  a 200 that is not the challenge shape is caught, not treated as a pass
PASS  an unreachable brain is named as unreachable, with its URL
PASS  a worker echoing the wrong plainToken back is rejected

zoom connect: all 48 tests passed
```

### CLI wiring — `node test/zoom-cli.test.mjs` (exit 0)

```
PASS  a manifest that has not declared zoom is refused
PASS  and nothing was written to the worker
PASS  and no webhook URL was printed
PASS  a partial environment is refused and names every missing value
PASS  the refusal says nothing was created or changed
PASS  the marketplace steps are printed so the client knows what to make
PASS  but still no webhook URL, because the worker holds no secret yet
PASS  and nothing was written
PASS  a Basic Zoom account is refused
PASS  the plan check runs BEFORE any secret is written
PASS  and no webhook URL was printed
PASS  a missing cloud_recording scope is refused, naming the scope
PASS  the scope check also runs before any secret is written
PASS  a Licensed account with the right scope connects cleanly
PASS  exactly four secrets are written, no more
PASS  they are the four Zoom issues
PASS  they carry the values supplied, unmodified
PASS  they go to the CLIENT'S own account and the manifest's own worker
PASS  the live handshake against the real worker route is confirmed
PASS  the webhook URL is printed only now, after all of that
PASS  the event to subscribe to is the transcript one
PASS  zoom is registered as a named source with no refresh cadence
PASS  no credential value is ever printed to the terminal
PASS  an undeployed worker is explained as an ordering problem, not an account problem
PASS  a worker answering with a different secret stops the wizard
PASS  and the client is told explicitly NOT to paste the URL into Zoom yet
PASS  so the URL and the event steps are never printed on that path
PASS  a worker running a build with no Zoom route/secret is named as needing a redeploy
PASS  an unreachable brain does not undo a good connection
PASS  it warns and explains why it is harmless
PASS  the webhook steps are still printed
PASS  disconnect removes all four secrets
PASS  from the same client account and worker
PASS  and says plainly that the webhook now refuses deliveries
PASS  it names the Zoom-side step it cannot do for the client
PASS  and points at forget for the transcripts already loaded
PASS  the result reports what it removed
PASS  secrets that were never set are not treated as a failure
PASS  a secret that will not delete is a hard failure, naming which one
PASS  and warns that Zoom may still be able to deliver transcripts

zoom cli: all 44 tests passed
```

The single most load-bearing test in this package is
`the CLI's challenge and the real worker route agree on the answer`. It points
`verifyLiveWebhookEndpoint` at the actual `handleZoomWebhook`, not a fake. Two
halves of a challenge-response tested only against their own fakes would agree
with themselves forever while failing in front of a client.

### Full suite — `npm test`

Run with stdout+stderr redirected to a log and the exit code captured to a
SEPARATE file, then read back from that file (never piped through `tail`,
which masks the real exit code):

```
$ npm test > /tmp/wp08-final.log 2>&1; echo $? > /tmp/wp08-final.exit
$ cat /tmp/wp08-final.exit
0
$ grep -c '^PASS' /tmp/wp08-final.log ; grep -c '^FAIL' /tmp/wp08-final.log
2296
0
$ grep -E "zoom.*all [0-9]+ tests passed" /tmp/wp08-final.log
zoom connect: all 48 tests passed
zoom cli: all 44 tests passed
zoom: all 71 tests passed
$ tail -3 /tmp/wp08-final.log
PASS  capability-link safety advances the durable gate version

secret-scan v4 (js): all tests passed
```

**Exit code 0, read back from `/tmp/wp08-final.exit`, not inferred from a
pipeline** — piping to `tail` would report `tail`'s exit code, not the
suite's. 2296 `PASS` lines, 0 `FAIL` lines. The `node --test` blocks inside
the chain (eval suite, webauthn/sessions/owner-auth) report separately and
sum to: tests 140, pass 140, fail 0. Baseline before this package was 2133
PASS; the 163 added are this package's 71 + 48 + 44.

## What could NOT be proven, and why

**There is no paid Zoom account in this environment, so nothing below was
faked to look otherwise.** These are unprovable here, not skipped:

1. **The package's own acceptance criterion.** No real Zoom meeting has
   produced a cited transcript document. Not attempted, not simulated.
2. **A real signature from Zoom.** Verification is proven against HMACs
   recomputed independently with `node:crypto`, so a bug in the verifier
   cannot agree with itself — but never against bytes Zoom actually signed.
3. **Zoom's real `endpoint.url_validation` POST**, the one the Marketplace
   fires when the URL is saved. Our challenge is the same shape and the worker
   answers it, but Zoom's actual save-time validation has never run.
4. **Zoom's real VTT dialect.** The fixtures are hand-written to the WebVTT
   spec and to what the reference's parsers imply Zoom emits. A real Zoom
   transcript may carry cue identifiers, styling or speaker formats the
   fixtures do not represent. This is the most likely place the first live run
   finds something.
5. **The real `/v2/meetings/{uuid}/recordings` response**, and whether the
   double-encoded uuid behaves as documented against Zoom's live router.
6. **Whether `/v2/users/me` is readable with only
   `cloud_recording:read:admin`.** Unknown, and deliberately so: the code
   handles both outcomes and the plan check degrades to a named unknown rather
   than asserting one. Which one Zoom actually does is a live question.
7. **The transcript download** with `?access_token=` appended to Zoom's signed
   URL.
8. **Plan type values in the wild.** `1 = Basic`, `2 = Licensed`,
   `3 = On-prem` is taken from the reference's own code and comment, not
   observed here.
9. **Which unit `x-zm-request-timestamp` actually uses.** Both are accepted
   (see the section above), so this cannot break the connector either way, but
   the answer itself is unverified here.
10. **A live Cloudflare secret write.** The four-secret PUT is driven against a
   recording closure; the underlying call path is the same one `brain secrets`
   already uses in production.

### What was deliberately NOT done (scope, not omission)

- No cron, sweep or backlog processor, and therefore no claim-row dedupe
  table. Webhook-only.
- No backfill: recordings made before connecting are not loaded, ever.
- No downstream CRM pipeline (call analysis, Drive filing, lead creation).
- No participant fetch — it needs a different Zoom permission family and the
  reference's version fails soft, so it was never forced to be scoped
  correctly. Dropped rather than half-shipped.
- No write scopes requested anywhere.
- Nothing under `~/CloudflareWorkers` was modified; it was read only.
- **No `package.json` version bump and no `CHANGELOG.md` heading**, per the
  merge discipline for three parallel branches. The owner-voice paragraph is
  below for the integrator to consolidate.

## The exact live verification, for the owner to run

Everything here needs a **paid (Licensed) Zoom seat**. Steps 1 to 6 are the
real end-to-end proof; step 0 is runnable today against any deployed brain.

```bash
# 0. Provable with no Zoom account at all: the webhook fails closed and
#    refuses unsigned bodies. Run against a deployed brain.
curl -sS -o /dev/null -w '%{http_code}\n' -X POST https://<brain-domain>/api/webhooks/zoom \
  -H 'content-type: application/json' \
  --data '{"event":"recording.transcript_completed","payload":{"object":{"uuid":"x"}}}'
#    503 = no ZOOM_WEBHOOK_SECRET_TOKEN set yet (fail closed).
#    401 = secret is set and the unsigned body was refused. Both are correct.
#    A 200 here would be a bug worth stopping for.

# Prerequisites for the rest, all in the CLIENT'S own accounts:
#   - a paid (Licensed) Zoom seat
#   - Zoom Settings > Recording > Cloud recording ON, with Audio transcript ON
#   - the brain worker deployed:  node brain.mjs deploy <manifest>
#   - "zoom": { "enabled": true } under "corpora" in the manifest

# 1. Print the exact Zoom Marketplace steps (safe: writes nothing).
node brain.mjs connect zoom <manifest>

# 2. Create the Server-to-Server OAuth app, scope cloud_recording:read:admin
#    (optionally also user:read:admin), Activate it, copy the four values.
#    Supply them through the approved launcher/secret manager, never on the
#    command line, then:
node brain.mjs connect zoom <manifest>
#    Expect, in this order:
#      checking these credentials against Zoom
#      Zoom plan: Licensed. Cloud recording is available on this account.
#      Zoom credentials verified, and the recording scope is granted
#      four Zoom secrets written to <worker> in the client's own Cloudflare account
#      the brain answered Zoom's validation challenge correctly. The URL will save.
#      ...then the Event Subscription steps, with the URL.

# 3. In the Zoom app: Feature > Event Subscriptions > add the printed URL,
#    click Validate (it MUST save), add ONLY recording.transcript_completed,
#    Save, Activate.

# 4. Hold a short Zoom meeting with cloud recording on. End it. Wait for
#    Zoom's transcript to finish — typically several minutes, longer for a
#    long meeting. This lag is Zoom's.

# 5. Prove it landed.
node brain.mjs sources <manifest>     # expect a zoom row, document_count 1
node brain.mjs ask <manifest>         # ask about something said on the call;
                                      # expect a cited answer from the transcript

# 6. Prove the undo, both halves.
node brain.mjs forget <manifest> --source zoom      # removes the transcripts
node brain.mjs disconnect zoom <manifest>           # removes the four secrets;
                                                    # rerun step 0, expect 503
```

If step 3's Validate fails even though step 2 said the challenge was answered
correctly, the cause is almost certainly that the URL typed into Zoom is not
the URL step 2 printed. Step 2 already proved the worker answers correctly at
that exact URL.

## Owner-voice changelog paragraph (for the integrator to consolidate)

**Your brain can now read your Zoom calls, automatically.**

If your Zoom account is on a paid plan and records to the cloud, `brain
connect zoom <manifest>` sets up a direct line: every time Zoom finishes
writing a transcript, it tells your brain, and your brain fetches and stores
that transcript on its own. No saving files, no dragging anything into a
folder. The Zoom app, the credentials and the recordings all stay in your
account — the setup walks you through creating the app in your own Zoom, and
the four values it gives you are written as secrets on your own worker.

The honest limits, stated up front. A paid (Licensed) Zoom seat is required:
Zoom's free tier cannot record to the cloud at all, so there is nothing to
transcribe, and the setup checks your plan and refuses rather than connecting
you to silence. Only NEW recordings arrive — nothing goes back and collects
calls you recorded before connecting; for those, saving the transcript into a
Drive folder you already ingest still works. Transcripts appear a few minutes
after the call ends, on Zoom's schedule rather than instantly. And this loads
the transcript only: not the audio, not the video, and no summary of the call.
It is the named source `zoom`, so `brain sources` shows it and `brain forget
<manifest> --source zoom` takes every transcript back out. `brain disconnect
zoom <manifest>` shuts the door.

One thing worth knowing: this has been built and tested thoroughly, but it has
not yet run against a real Zoom account. You will be the first. Expect the
first call to be the one that finds anything the tests could not.

## Files touched in this package

New:

- `worker/src/lib/zoom.js`
- `worker/test/zoom.test.mjs`
- `worker/test/fixtures/zoom/cue-numbers.vtt`
- `worker/test/fixtures/zoom/header-and-note.vtt`
- `worker/test/fixtures/zoom/multi-line-cue.vtt`
- `worker/test/fixtures/zoom/unattributed.vtt`
- `connectors/zoom.mjs`
- `test/zoom-connect.test.mjs`
- `test/zoom-cli.test.mjs`
- `evidence/WP-08.md`

Modified:

- `worker/src/index.js` (route + `SOURCE_KINDS`)
- `brain.mjs` (connect/disconnect zoom, dispatchers, help, support routing)
- `manifest.schema.json`, `templates/brain.manifest.json` (`corpora.zoom`)
- `support-journal.mjs` (`zoom` source)
- `onboarding/07-ingest-source-matrix.md` (Zoom row + section, Meet
  cross-reference corrected)
- `test/package-privacy.test.mjs` (allowlist: `worker/src/lib/zoom.js`,
  `connectors/zoom.mjs`)
- `package.json` (three test files added to the chain; **no version bump**)

## Note for the integrator

This branch was cut from `wave0/connector-gaps` at `cc6602b`. That branch has
since gained two commits of its own (`44ca0a0` provider routing, `9dd1e42`
spend cap). A `git diff wave0/connector-gaps` from here therefore shows
`worker/src/lib/core.js`, `worker/test/spend-cap.test.mjs`,
`worker/test/provider-routing.test.mjs` and `evidence/ISSUE-05-spend-cap.md`
as removals. **Nothing was deleted by this package.** Those files simply
postdate the branch point; `git diff --name-only bd3f1f4~1..HEAD` lists every
file this package actually touched, and none of them are on that list.

Per the merge discipline for three parallel branches, `package.json`'s version
is untouched and no `CHANGELOG.md` heading was added. The one expected
conflict is `package.json`'s `test` script, where three test files were
appended to the chain: `test/zoom-connect.test.mjs`, `test/zoom-cli.test.mjs`
and `worker/test/zoom.test.mjs`. The owner-voice changelog paragraph is above.

## Commits on `wave1/wp08-zoom`

```
da559e2 Two failure modes the reference could hide but this design cannot
76139b8 WP-08 evidence: verbatim output, and what a paid Zoom account would prove
dda494a Declare zoom in the install record, and teach support it exists
059ca8d Matrix: Zoom moves to Built, with the limits stated first
9347762 Wire Zoom into the CLI: brain connect zoom, brain disconnect zoom
32dcdf3 Zoom connect: the credential probe and the handshake self-check
bd3f1f4 WP-08: the Zoom transcript webhook on the client's own worker
```

(Oldest last. The evidence file is itself carried by the last two commits,
so this list is written one commit behind its own final state; the seventh
commit is the one that updates this section.)
