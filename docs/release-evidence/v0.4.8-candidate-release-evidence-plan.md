# v0.4.8 candidate release evidence plan

- Status: planning only; held
- Prepared: 2026-09-11
- Candidate source commit: unbound until all intended code and documentation
  are frozen
- Candidate package: not built or sealed
- Field execution: none
- Release state: no tag, release asset, public update, deployment, or customer
  execution exists for 0.4.8
- Historical lineage: the superseded
  [v0.4.7 plan](v0.4.7-candidate-release-evidence-plan.md) records the earlier
  held composition; no field campaign was run from it and 0.4.7 was never
  tagged, published, or offered as a customer update
- Packaging: this path is outside the package `files` allowlist

## Proof boundary

This file is a public, synthetic plan. It is not a field receipt, closes no
incident, renews no deferral, and authorizes no release or live-system action.
The 0.4.8 identity prevents changed candidate bytes from inheriting the prior
0.4.7 planning identity or any evidence associated with it.

The final source SHA is deliberately absent. Before any field preparation or
campaign begins, replace the unbound source line above in a reviewed change
with the exact frozen commit. Then build one package from that commit and bind
every accepted receipt to all of these values:

- final Git commit;
- package version, filename, byte count, and SHA-256;
- actual installed `brain` entrypoint and reported version;
- platform, architecture, Node major, and scenario;
- commands, exit codes, interruption point, and aggregate before/after counts;
- observed retrieval, refusal, or recovery result; and
- everything the run did not prove.

A later executable-code, dependency, generated-bundle, migration, or release
runbook change invalidates affected evidence until it is repeated on the new
bytes. Credentials, account identifiers, private hostnames, customer content,
personal names, and secret-bearing screenshots never belong in a repository
receipt.

## Current held state

The update registry currently contains 39 stable incident rows: 35 unresolved
and four closed on reviewed evidence. No version-scoped deferrals are active.
Those counts describe the current registry, not release readiness. All 35
unresolved rows block a 0.4.8 release write unless a later, separately reviewed
change supplies the row's accepted evidence or a permitted exact-version
deferral.

The schemas 43 through 46 work remains narrowly scoped. Eligible
single-record local files can carry exact-original binding, a complete result
family can be sealed, and one exact owner-selected original can enter the
accepted-resolution proof only after all required checks pass. Every such
receipt states `whole_source_complete: false`. Other producers and ambiguous
multi-record exports remain unbound. The legacy whole-source repair stays
read-only, OCR is off in the one-target lane, and none of this is field proof.
Schema 46 additionally requires each new observation to append against its
exact reviewed predecessor. Deterministic release evidence must include a
two-machine same-head loser, exact lost-response replay with no duplicate row,
later-exclusion demotion, and recovery that preserves the exclusion as the
non-reactivatable current head.

## Required order

### 1. Freeze one exact candidate

Keep `package.json`, both root lockfile versions, the manifest template, the
Worker version, README placeholder URLs, newest changelog heading, and dynamic
current-version checks at 0.4.8. The enforcing update audit must reject 0.4.6
as already live and 0.4.7 as a retired held candidate identity. Record the
final reviewed SHA only after every intended file is present.

### 2. Prepare and seal without live credentials

From a clean checkout of that exact final SHA, print the no-write plan first:

```bash
node scripts/field-prepare.mjs --plan --json --expect-sha <final-candidate-sha>
```

Then run the complete SHA-bound offline preparation and exact-package release
checks. The receipt must show the package digest, package allowlist, clean
install entrypoint, current-version alignment, frontend bundle parity, offline
audit result, and every failing or skipped gate. A held release audit remains a
held result even when all deterministic tests pass.

### 3. Complete physical owner-platform gates

Use the one sealed package on the required physical Windows and macOS systems.
Preserve the exact package identity across PowerShell, DPAPI, browser sign-in,
owner onboarding, interruption, and restart evidence. Hosted CI supports these
receipts but cannot replace physical Windows ARM64 or owner-controlled browser
and passkey ceremonies when an incident explicitly requires them.

### 4. Complete disposable Cloudflare update and recovery gates

Prepare a separately reviewed disposable-resource plan and exact teardown
targets before creating anything. After the named account, resources, writes,
evidence capture, and teardown are explicitly approved, prove the accelerated
paused-update vector path using exactly 3,201 deterministic fictional one-chunk
records. The source-only runner and unpacked package must both be bound to the
exact frozen candidate. The private binding records the runner and manifest
SHA-256, package archive SHA-256 and byte count, shared runtime-content
fingerprint, candidate commit, and fixed corpus SHA-256. Use the reviewed
`--stop-after-bootstrap-progress after-first-durable-progress` control to stop
only after the first durable non-final bootstrap progress, then rerun the same
approved command to prove the unchanged epoch, cursor hash, corpus, target, and
paused Worker before any new bootstrap work. Before the state machine starts,
bind its private journal to this field-proof mode and paired stage control; the
completed rebuild evidence must later bind both private receipt hashes. Use
three identical invocations
with that option and `--stop-after-stage rebuild_vectorize`: interrupt inside
bootstrap; prove the unchanged resume boundary, finish the rebuild, and stop at
the completed stage; then continue past that stage without replay. The
one-epoch ledger must contain exactly four ordered rows, batch numbers 1, 2, 3,
and 4 with row counts 1,000, 1,000, 1,000, and 201. This accounts for exactly
3,201 chunks without claiming that more than 3,000 were simultaneously pending.
The adapter must use field receipt contract 3 so a final provider-count lag
cannot rebase the verified epoch before the independent ledger observation.
Every retry with an existing resume receipt must recheck the live epoch and
cursor before another bootstrap POST. An already verified lost response must
be reconciled without replay, and an active retry after an ambiguous promotion
must prove the same completed ledger before recording completion.
Finish with all of the following:

- writes returned to active mode through the reviewed updater;
- zero queued or failed outbox work;
- opening and closing health identify Brain `v048-field-proof` at schema 46;
- exact D1 document and expected-vector counts, with both document and chunk
  count flags explicitly exact;
- independently observed Vectorize parity; and
- cited retrieval from the restored synthetic corpus.

Do not use a customer Brain, export a live Brain, restore a bookmark first,
manually clear paused mode, or treat HTTP success and queue movement as final
proof. Provisioning, deployment, evidence capture, and teardown remain separate
approval-gated actions.

The seeder, private receipt helper, interruption hook, and deterministic tests
on a held branch are implementation evidence only. No v0.4.8 provider or field
execution has occurred, so this step remains open and cannot support a release
claim.

The current helper and seeder execute only on POSIX systems. Native Windows
execution is refused before credential or provider access because the helper
does not yet verify a current-user-only DACL. This field limitation does not
waive any physical Windows candidate gate, and the seeder's local no-write
`--plan` mode remains available.
On macOS, any extended ACL on the selected receipt directory or a marker,
temporary, or final receipt file is a refusal. Empty files are checked before
receipt bytes are written, and caller-owned ACLs are never changed.

### 5. Complete provider and permanent-host gates separately

Run only the provider and permanent-host scenarios required by the current
incident registry. A fixture, configured connector, local API response, or
earlier candidate rehearsal does not prove a real-provider owner journey.
General bank invitations, real-data Plaid work, WhatsApp live pairing, and any
customer execution remain held until their own prerequisites and approvals are
satisfied.

## Evidence adjudication

The acceptance text in `docs/update-incidents.json` is authoritative. The
superseded 0.4.7 plan may help explain historical campaign decomposition, but
it is neither current 0.4.8 evidence nor authority to weaken a row. For every
current row, the only permitted outcomes are:

- **Close:** attach reviewed, sanitized evidence from the exact final 0.4.8
  package, then propose `verified` in a separate change.
- **Defer:** use only a permitted closed-enum cause in a separately reviewed
  deferral that names exactly 0.4.8 and clearly states what ships unproven.
- **Block:** leave the row unresolved and do not cut the release.

Only after the exact-SHA suite, package-byte comparison, required physical and
disposable campaigns, registry audit, and served guide/hash verification all
pass may a release PR claim readiness. Merge, tag, publication, manifest
change, deployment, customer test, and teardown are separate actions and need
their own authority.
